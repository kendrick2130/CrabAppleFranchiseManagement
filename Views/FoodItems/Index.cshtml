@model IEnumerable<FranchiseManagement.Models.FoodItem>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Name)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Price)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.FoodItemDescription)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Name)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Price)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.FoodItemDescription)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.FoodItemID">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.FoodItemID">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.FoodItemID">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
